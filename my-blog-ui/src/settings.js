module.exports = {
    title: 'MyBlog',

    /**
     * 是否显示 tagsView
     */
    tagsView: true,

    /**
     * 是否固定头部
     */
    fixedHeader: false,

    /**
     * 是否显示 logo
     */
    sidebarLogo: true,

    /**
     * @type {string | Array} 'production' | ['production', 'development']
     * @description 需要显示错误日志组件
     * 默认在生产环境中使用
     * 如果想要在开发环境中使用，可以传入['production', 'development']
     */
    errorLog: 'production'
}